import { RoundBorderShadowDirective } from './round-border-shadow.directive';

describe('RoundBorderShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBorderShadowDirective();
    expect(directive).toBeTruthy();
  });
});
